{
    "name": "@automoto/mono",
    "version": "1.0.0",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "turbo run dev",
        "fmt": "prettier --write ."
    },
    "devDependencies": {
        "prettier": "^2.8.1",
        "turbo": "^1.6.3"
    }
}
