class Environment {
    getVariable(name: string): string | null {
        return "placeholder"
    }

    getSecret(name: string): string | null {
        return "placeholder"
    }
}

export default Environment